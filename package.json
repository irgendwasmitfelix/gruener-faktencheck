{
  "name": "gruener-faktencheck",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "npm run generate && vite build",
    "preview": "vite preview",
    "generate": "python scripts/generate_sitemap.py && python scripts/generate_rss.py && python scripts/generate_static_html.py",
    "setup-hooks": "node setup-hooks.js"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-helmet": "^6.1.0",
    "react-router-dom": "^7.11.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^4.0.0"
  }
}
